/* Barbending Schedule for beam */

const CuttingLengthBeam = (lClear, barDia) => {
    const DevLength = barDia * 50;
    const length = lClear + (2*DevLength);
    return(
        length
    )

};

const CuttingLengthStirrups = (barDia)=> {
    var elongatedBend = number1 * typeofBend + onethirtyBend * 2
    var hookLength =  9*barDia*2
    const stirrupsLength = pStirrups + hookLength - elongatedBend

    return stirrupsLength
};